// cardGridStyle.ts
import SDims from './dimensions';

const margin = SDims.Width5p;
const marginLeft = SDims.Width5p;
const gap = SDims.Width5p;
const imageWidth = SDims.Width1_4f;
const imageHeight = imageWidth;

export const cardGridStyle = {
  margin,
  marginLeft,
  gap,
  imageWidth,
  imageHeight,
};
