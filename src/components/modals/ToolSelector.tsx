import React from 'react';
import { View, Text } from 'react-native';

const ToolSelector = () => (
  <View style={{ backgroundColor: 'lightcoral', padding: 10 }}>
    <Text>Tool Selector</Text>
  </View>
);

export default ToolSelector;
