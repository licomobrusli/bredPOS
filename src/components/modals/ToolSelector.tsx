import React from 'react';
import { View, Text } from 'react-native';
import SDims from '../../config/dimensions';

const ToolSelector = () => (
  <View style={{ backgroundColor: 'lightcoral', padding: SDims.D10px }}>
    <Text>Tool Selector</Text>
  </View>
);

export default ToolSelector;
