import React from 'react';
import { View, Text } from 'react-native';

const CutSelector = () => (
  <View style={{ backgroundColor: 'lightyellow', padding: 10 }}>
    <Text>Cut Selector</Text>
  </View>
);

export default CutSelector;
