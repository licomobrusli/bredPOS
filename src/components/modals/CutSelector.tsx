import React from 'react';
import { View, Text } from 'react-native';
import SDims from '../../config/dimensions';

const CutSelector = () => (
  <View style={{ backgroundColor: 'lightyellow', padding: SDims.D10px }}>
    <Text>Cut Selector</Text>
  </View>
);

export default CutSelector;
